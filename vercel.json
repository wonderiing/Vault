{
    "buildCommand": "pip install -r requirements.txt && python scripts/sync_notes.py && mkdocs build",
    "outputDirectory": "site",
    "installCommand": "pip install -r requirements.txt",
    "framework": null
}